using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoneyImageUI : MonoBehaviour
{
    public Vector2 myAnchoredPostion => GetComponent<RectTransform>().localPosition;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
